反向代理服务器：
	真正的服务器不允许其它客户端访问，只允许指定的服务器访问，因此客户端如果需要访问
服务器就必须通过其指定的服务器（代理服务器）访问，等于加了一层中转

正向代理服务器：
	也是在客户端和服务器之间增加一个服务器，与反向代理不同是，正向代理是隐藏客户端，
而反向代理是隐藏服务器

总结：正向代理服务器是客户端自己加的，不想让服务器知道自己的真实信息
      反向代理服务器是服务器加的，目的是不想让客户端知道自己的真实信息

Nginx:
	http服务器软件，采用模块化结构设计，支持正向、反向代理、虚拟主机、url重写、压缩传输、ssl加密传输
	